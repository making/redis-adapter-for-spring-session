# The region between the markers is quoted verbatim in README.md as what the in-memory
# backend is tuned with. ReadmeInMemoryConfigurationExamplesTests starts a real server
# from it, so a property name that is subtly wrong - which would bind to nothing and
# change nothing - fails here rather than in a deployment.

# tag::server-in-memory[]
redis-adapter.in-memory.sweeper-enabled=true
redis-adapter.in-memory.sweep-interval=1s
# end::server-in-memory[]

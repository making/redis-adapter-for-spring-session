<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>am.ik.redis</groupId>
		<artifactId>redis-adapter-for-spring-session</artifactId>
		<version>0.2.0-SNAPSHOT</version>
	</parent>

	<artifactId>redis-adapter-for-spring-session-server-dynamodb</artifactId>
	<name>redis-adapter-for-spring-session-server-dynamodb</name>
	<description>The Redis adapter server that keeps its sessions in DynamoDB</description>

	<dependencies>
		<dependency>
			<groupId>am.ik.redis</groupId>
			<artifactId>redis-adapter-for-spring-session-server</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>am.ik.redis</groupId>
			<artifactId>redis-adapter-for-spring-session-dynamodb</artifactId>
			<version>${project.version}</version>
		</dependency>
		<!-- The DynamoDbClient the backend is handed comes from Spring Cloud AWS, so where
		     DynamoDB is and how requests are signed is the spring.cloud.aws.* properties'
		     business — region, credentials or the default provider chain, and the endpoint
		     override that points a test or a local run at an emulator. The SDK's own HTTP
		     clients are excluded here for the same reason as in the store module: the
		     backend speaks over url-connection-client, and with more than one sync client
		     on the classpath the SDK refuses to pick. -->
		<dependency>
			<groupId>io.awspring.cloud</groupId>
			<artifactId>spring-cloud-aws-starter-dynamodb</artifactId>
			<version>${spring-cloud-aws.version}</version>
			<exclusions>
				<exclusion>
					<groupId>software.amazon.awssdk</groupId>
					<artifactId>netty-nio-client</artifactId>
				</exclusion>
				<exclusion>
					<groupId>software.amazon.awssdk</groupId>
					<artifactId>apache-client</artifactId>
				</exclusion>
				<exclusion>
					<groupId>software.amazon.awssdk</groupId>
					<artifactId>apache5-client</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<!-- The harness the server module publishes: the shipped configuration under a real
		     Lettuce client, the key names Spring Session writes, and the benchmark. Running
		     it here is what says this backend serves sessions, rather than only that it
		     compiles against the SPI. -->
		<dependency>
			<groupId>am.ik.redis</groupId>
			<artifactId>redis-adapter-for-spring-session-server</artifactId>
			<version>${project.version}</version>
			<type>test-jar</type>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-session-data-redis</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- AWS publishes no DynamoDB you can run, so a Spring Session application is driven
		     against the Floci emulator from here — the honest limits of that are recorded in
		     .docs/design/architecture.md §12.6. -->
		<dependency>
			<groupId>io.floci</groupId>
			<artifactId>testcontainers-floci</artifactId>
			<version>${testcontainers-floci.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>am.ik.maven</groupId>
				<artifactId>nullability-maven-plugin</artifactId>
				<extensions>true</extensions>
			</plugin>
			<plugin>
				<groupId>io.spring.javaformat</groupId>
				<artifactId>spring-javaformat-maven-plugin</artifactId>
			</plugin>
			<!-- The runnable server is attached under the "exec" classifier so that the plain
			     jar stays a jar: its configuration classes are also usable from an application
			     that would rather run the adapter inside itself. -->
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<version>${spring-boot.version}</version>
				<configuration>
					<classifier>exec</classifier>
					<mainClass>am.ik.redis.adapter.boot.dynamodb.DynamoDbRedisAdapterServerApplication</mainClass>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>repackage</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
